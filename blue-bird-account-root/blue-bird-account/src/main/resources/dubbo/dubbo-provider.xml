<beans xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
       xmlns:dubbo="http://dubbo.apache.org/schema/dubbo"
       xmlns="http://www.springframework.org/schema/beans"
       xsi:schemaLocation="http://www.springframework.org/schema/beans http://www.springframework.org/schema/beans/spring-beans-4.3.xsd
       http://dubbo.apache.org/schema/dubbo http://dubbo.apache.org/schema/dubbo/dubbo.xsd">

    <!-- 提供方应用信息，用于计算依赖关系 -->
    <dubbo:application name="blue-bird-account" logger="slf4j">
        <dubbo:parameter key="qos.enable" value="false"/>
        <dubbo:parameter key="qos.accept.foreign.ip" value="false"/>
        <dubbo:parameter key="qos.port" value="55555"/>
    </dubbo:application>


    <!-- 声明需要暴露的账户服务接口 -->
    <dubbo:service retries="0" interface="com.zhangpeng.account.api.service.AccountTransactionService"
                   ref="accountTransactionService"  timeout="60000"/>
    <dubbo:service retries="0" interface="com.zhangpeng.account.api.service.AccountService"
                   ref="accountService"  timeout="60000"/>
    <dubbo:service retries="0" interface="com.zhangpeng.account.api.service.AccountQueryService"
                   ref="accountQueryService"  timeout="60000"/>
    <dubbo:service retries="0" interface="com.zhangpeng.account.api.service.AccountHistoryService"
                   ref="accountHistoryService"  timeout="60000"/>
</beans>